1. git init  // to create or initialize a git repositry

2. git clone <url>  //to establish connections

3. git status

4. git checkout <new branch name>   //to change the branch

5. git add . //to track 

6. git commit -m "whatever." //to save all changes 
 
7. git push   //  push all the changes on cloud
